# Model Defaults Configuration
# Defines default model tiers per harness.
# Use "model: default", "model: default-small", or "model: default-large"
# in agent/skill files to auto-resolve to harness-specific models.
#
# Format:
#   [harness]
#   tier = model-name
#   tier.field = value   (additional fields set when tier is used)
#
# Each harness has its own reasoning field:
#   Codex:   model_reasoning_effort (low/medium/high)
#   Claude:  effort (low/medium/high/max)
#   Copilot: reasoning_effort (low/medium/high)
#   Gemini:  thinkingLevel (LOW/MEDIUM/HIGH)

[codex]
default-small = gpt-5-mini
default-small.model_reasoning_effort = medium
default = gpt-5.5
default.model_reasoning_effort = low
default-large = gpt-5.5
default-large.model_reasoning_effort = high

[claude]
default-small = claude-haiku-4.5
default-small.effort = medium
default = claude-sonnet-4.6
default.effort = medium
default-large = claude-opus-4.6
default-large.effort = high

[copilot]
default-small = gpt-5-mini
default-small.reasoning_effort = medium
default = claude-opus-4.6
default.reasoning_effort = high
default-large = claude-opus-4.6
default-large.reasoning_effort = high

[gemini]
default-small = gemini-3.1-flash-lite
default-small.thinkingLevel = MEDIUM
default = gemini-3.1-flash
default.thinkingLevel = MEDIUM
default-large = gemini-3.1-pro
default-large.thinkingLevel = HIGH
